package buisness.Listeners;
 
 
import org.testng.ITestResult;
import org.testng.TestListenerAdapter;
 
 
public class Screenshot extends TestListenerAdapter {
 
  @Override
	public void onTestFailure(ITestResult tr) {
		
	/*	File scrFile = ((TakesScreenshot)Setup.driver).getScreenshotAs(OutputType.FILE);
		DateFormat dateFormat = new SimpleDateFormat("dd_MMM_yyyy__hh_mm_ssaa");
		String destDir = System.getProperty("user.dir")+"\\Screenshots\\";
		new File(destDir).mkdirs();
		String destFile = "Failure_" + dateFormat.format(new Date()) + ".png";
 
        try {
			FileUtils.copyFile(scrFile, new File(destDir + "/" + destFile));
		} catch (IOException e) {
			e.printStackTrace();
			
		}
		Setup.APPLICATION_LOGS.info("Saved <a href=../screenshots/" + destFile + ">Screenshot</a>");*/
	}
  
  @Override
	public void onTestSuccess(ITestResult tr) {
		
		/*File scrFile = ((TakesScreenshot)Setup.driver).getScreenshotAs(OutputType.FILE);
		DateFormat dateFormat = new SimpleDateFormat("dd_MMM_yyyy__hh_mm_ssaa");
		String destDir = System.getProperty("user.dir")+"\\Screenshots\\";
		new File(destDir).mkdirs();
		String destFile = "Success_" + dateFormat.format(new Date()) + ".png";
 
        try {
			FileUtils.copyFile(scrFile, new File(destDir + "/" + destFile));
		} catch (IOException e) {
			e.printStackTrace();
		}
        TestListenerAdapter t = new TestListenerAdapter();
        Setup.APPLICATION_LOGS.info("Saved <a href=../screenshots/" + destFile + ">Screenshot</a>");*/
	}
 
}
